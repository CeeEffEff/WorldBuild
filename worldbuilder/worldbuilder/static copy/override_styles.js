document.addEventListener('DOMContentLoaded', function() {
    var chartElement = document.getElementById('map-graph');
    if (chartElement) {
        chartElement.style.height = ''; // Reset the height to allow dynamic sizing
    }
});